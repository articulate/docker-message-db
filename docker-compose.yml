services:
  db:
    build:
      args:
        MESSAGE_DB_VERSION: ${MESSAGE_DB_VERSION:-1.3.0}
        POSTGRES_VERSION: ${POSTGRES_VERSION:-16.1}
      context: .
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - '5432'
